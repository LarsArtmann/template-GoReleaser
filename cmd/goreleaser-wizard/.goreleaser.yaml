# GoReleaser configuration
# Generated by goreleaser-wizard
# https://goreleaser.com

version: 2

project_name: concurrent-test-0

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go test ./...

builds:
  - id: wizard-concurrent-0186738388
    main: .
    binary: wizard-concurrent-0186738388
    
    env:
      - CGO_ENABLED=
    
    goos:

    goarch:


    # Ignore certain platform combinations
    ignore:
      - goos: darwin
        goarch: 386
      - goos: windows
        goarch: arm64



archives:
  - id: default
    name_template: "concurrent-test-0_v0.1.0_{{.Os}}_{{.Arch}}"
    
    format_overrides:
      - goos: windows
        format: zip
    
    files:
      - LICENSE*
      - README*
      - CHANGELOG*

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  version_template: "v0.1.1-next-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: "owner"
    name: "repo"
  
  draft: false
  prerelease: auto
  mode: append
  
  footer: |
    ## Installation
    Download the appropriate archive for your platform from the assets below.


dockers:
  - image_templates:
      - "/concurrent-test-0:v0.1.0"
      - "/concurrent-test-0:v0"
      - "/concurrent-test-0:latest"
    
    dockerfile: Dockerfile
    
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created=2026-01-25"
      - "--label=org.opencontainers.image.title=concurrent-test-0"
      - "--label=org.opencontainers.image.revision=unknown"
      - "--label=org.opencontainers.image.version=v0.1.0"
      - "--platform=linux/amd64"

